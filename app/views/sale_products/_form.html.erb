<%= form_with(model: sale_product, local: true) do |form| %>
  <% if sale_product.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(sale_product.errors.count, "error") %> prohibited this sale_product from being saved:</h2>

      <ul>
      <% sale_product.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :sale_id %>
    <%= form.text_field :sale_id %>
  </div>

  <div class="field">
    <%= form.label :product_id %>
    <%= form.text_field :product_id %>
  </div>

  <div class="field">
    <%= form.label :comment %>
    <%= form.text_area :comment %>
  </div>

  <div class="field">
    <%= form.label :active %>
    <%= form.check_box :active %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
