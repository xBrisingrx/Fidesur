<%= form_with(model: provider_role, local: true) do |form| %>
  <% if provider_role.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(provider_role.errors.count, "error") %> prohibited this provider_role from being saved:</h2>

      <ul>
      <% provider_role.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_field :description %>
  </div>

  <div class="field">
    <%= form.label :active %>
    <%= form.check_box :active %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
